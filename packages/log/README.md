# `@saas/log`

Provider-agnostic logger. Pino is the default sink; swap to `console` /
`silent` (or your own transport) at runtime without touching call sites.

See [`SKILL.md`](../../.claude/skills/log/SKILL.md) for the full guide
(usage, conventions, extension).
